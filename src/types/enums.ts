export enum VIEW {
  MAIN = 'main',
  DICTIONARY = 'dictionary',
  GAMES = 'games',
  STATISTICS = 'statistics',
  SIGNIN = 'signin',
  SIGNOUT = 'signout',
}

export enum PATH {
  WORDS = 'words',
  USERS = 'users',
  AGGREGATED_WORDS = 'aggregatedWords',
  STATISTIC = 'statistic',
  SETTING = 'setting',
  SIGNIN = 'signin',
}

export enum GAMES {
  SPRINT = 'Sprint',
  AUDIO = 'Audio',
}

export enum RESULTS {
  ACCURACY = 'accuracy',
  WORDS = 'words',
}

export enum DICTIONARY {
  WORDS = 'words',
  HARD = 'hard',
}

export enum LEVEL {
  'A1',
  'A2',
  'B1',
  'B2',
  'C1',
  'C2',
}
