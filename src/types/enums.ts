export enum View {
  MAIN = 'main',
}

export enum PATH {
  WORDS = 'words',
  USERS = 'users',
  AGGREGATED_WORDS = 'aggregatedWords',
  STATISTIC = 'statistic',
  SETTING = 'setting',
  SIGNIN = 'signin',
}
